import {
	toDataStreamResponse,
	toDataStream,
	mergeIntoDataStream,
} from "./streams/langchain-adaptor";

export const LangChainAdapter = {
	toDataStreamResponse,
	toDataStream,
	mergeIntoDataStream,
};
